# Tracker---91-East
The radar sweep rotates and "pings" each target twice per second. Each ping adds Gaussian noise to the true position — those are the faded blobs you see. A Kalman filter (the dashed circle around each target) then fuses the noisy measurements over time to estimate the real position and velocity
